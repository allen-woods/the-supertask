FROM mongo:4.4.5-bionic 

# WORKDIR /
# COPY --chown=root:root ./init/authorization /usr/local/etc/mongodb/
# COPY --chown=root:root ./init/init.sh /usr/local/bin/

# RUN ["/bin/bash", "-c", "exec gosu root apt-get update"]
# RUN ["/bin/bash", "-c", "exec gosu root apt-get -y install curl"]
# RUN /usr/local/bin/init.sh